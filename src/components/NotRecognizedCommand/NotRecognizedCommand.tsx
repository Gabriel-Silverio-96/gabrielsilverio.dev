import styles from "./styles/NotRecognizedCommand.module.css";

const NotRecognizedCommand = () => (
	<h4 className={styles.message}>Not a recognized command</h4>
);

export default NotRecognizedCommand;